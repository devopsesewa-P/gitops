apiVersion: v1
kind: Service
metadata:
  name: gitops-service
  namespace: gitops
spec:
  type: NodePort
  selector:
    app: gitops
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30011
